/* 出行数据板 Dashboard 页面样式 */

.dashboard-page {
  min-height: 100vh;
  background: #EEF2FF;
}

/* 头部 - 使用圆角渐变 */
.dashboard-header {
  padding: 60rpx 40rpx 20rpx;
}

.dashboard-header-content {
  color: #FFFFFF;
}

.dashboard-title {
  font-size: var(--fs-48);
  font-weight: 700;
}

.dashboard-subtitle {
  font-size: var(--fs-26);
  padding-top: 5rpx;
}

/* 内容区 */
.dashboard-content {
  padding: 24rpx;
  padding-bottom: 100rpx;
  margin-top: -8rpx;
}

/* 部门筛选 */
.dashboard-filter {
  display: flex;
  gap: 16rpx;
  margin-bottom: 20rpx;
}

.dashboard-filter-picker {
  display: inline-flex;
  align-items: center;
  gap: 8rpx;
  background: #FFFFFF;
  padding: 16rpx 24rpx;
  border-radius: 12rpx;
  font-size: var(--fs-28);
  color: #1E293B;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
}

.dashboard-filter-arrow {
  font-size: var(--fs-22);
  color: #64748B;
}

/* Tab 切换 */
.dashboard-tabs {
  display: flex;
  background: #FFFFFF;
  border-radius: 16rpx;
  padding: 8rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
}

.dashboard-tab {
  flex: 1;
  text-align: center;
  padding: 20rpx 0;
  font-size: var(--fs-28);
  color: #64748B;
  border-radius: 12rpx;
  transition: all 0.3s ease;
}

.dashboard-tab.is-active {
  background: linear-gradient(135deg, #2563EB 0%, #4F46E5 100%);
  color: #FFFFFF;
  font-weight: 500;
}

/* 统计卡片网格 */
.dashboard-stats-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 16rpx;
  margin-bottom: 24rpx;
}

.dashboard-stat-card {
  background: #FFFFFF;
  border-radius: 20rpx;
  padding: 32rpx;
  text-align: center;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
}

.dashboard-stat-value {
  font-size: var(--fs-56);
  font-weight: 700;
  color: #2563EB;
  margin-bottom: 8rpx;
}

.dashboard-stat-label {
  font-size: var(--fs-26);
  color: #64748B;
}

/* 章节标题 */
.dashboard-section {
  margin-bottom: 24rpx;
}

.dashboard-section-title {
  font-size: var(--fs-30);
  font-weight: 600;
  color: #1E293B;
  margin-bottom: 16rpx;
  padding-left: 8rpx;
}

/* 当前外出人员列表 */
.dashboard-active-list {
  background: #FFFFFF;
  border-radius: 20rpx;
  overflow: hidden;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
}

.dashboard-active-item {
  display: flex;
  align-items: center;
  padding: 24rpx;
  border-bottom: 1rpx solid #F1F5F9;
}

.dashboard-active-item:last-child {
  border-bottom: none;
}

/* 使用全局 office-avatar 样式 */
.dashboard-active-avatar {
  margin-right: 20rpx;
}

.dashboard-active-info {
  flex: 1;
}

.dashboard-active-name {
  font-size: var(--fs-30);
  font-weight: 500;
  color: #1E293B;
  margin-bottom: 4rpx;
}

.dashboard-active-dest {
  font-size: var(--fs-26);
  color: #64748B;
}

.dashboard-active-meta {
  text-align: right;
}

.dashboard-active-mode {
  font-size: var(--fs-24);
  color: #64748B;
  margin-bottom: 4rpx;
}

.dashboard-active-time {
  font-size: var(--fs-24);
  color: #94A3B8;
}

/* 出行记录列表 */
.dashboard-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.dashboard-item {
  background: #FFFFFF;
  border-radius: 20rpx;
  padding: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
}

.dashboard-item-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
}

.dashboard-item-user {
  display: flex;
  align-items: center;
}

/* 使用全局 office-avatar 样式 */
.dashboard-item-avatar {
  margin-right: 16rpx;
}

.dashboard-item-name {
  font-size: var(--fs-30);
  font-weight: 500;
  color: #1E293B;
}

.dashboard-item-dept {
  font-size: var(--fs-24);
  color: #94A3B8;
}

.dashboard-item-status {
  font-size: var(--fs-24);
  padding: 6rpx 16rpx;
  border-radius: 8rpx;
  font-weight: 500;
}

.dashboard-item-body {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
  padding-top: 16rpx;
  border-top: 1rpx solid #F1F5F9;
}

.dashboard-item-row {
  display: flex;
  justify-content: space-between;
}

.dashboard-item-label {
  font-size: var(--fs-26);
  color: #64748B;
}

.dashboard-item-value {
  font-size: var(--fs-26);
  color: #1E293B;
}

/* 状态统计网格 */
.dashboard-status-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16rpx;
}

.dashboard-status-item {
  background: #FFFFFF;
  border-radius: 16rpx;
  padding: 24rpx;
  text-align: center;
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.04);
}

.dashboard-status-value {
  font-size: var(--fs-48);
  font-weight: 700;
  margin-bottom: 8rpx;
}

.dashboard-status-label {
  font-size: var(--fs-24);
  color: #64748B;
}

/* 图表条形图 */
.dashboard-chart-bars {
  background: #FFFFFF;
  border-radius: 20rpx;
  padding: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
}

.dashboard-chart-bar-item {
  display: flex;
  align-items: center;
  margin-bottom: 20rpx;
}

.dashboard-chart-bar-item:last-child {
  margin-bottom: 0;
}

.dashboard-chart-bar-label {
  width: 120rpx;
  font-size: var(--fs-26);
  color: #64748B;
  flex-shrink: 0;
}

.dashboard-chart-bar-wrapper {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.dashboard-chart-bar {
  height: 24rpx;
  background: linear-gradient(90deg, #2563EB 0%, #4F46E5 100%);
  border-radius: 12rpx;
  min-width: 20rpx;
  transition: width 0.5s ease;
}

.dashboard-chart-bar--dept {
  background: linear-gradient(90deg, #16A34A 0%, #059669 100%);
}

.dashboard-chart-bar-value {
  font-size: var(--fs-24);
  color: #1E293B;
  font-weight: 500;
  min-width: 40rpx;
}

/* 空状态 */
.dashboard-empty {
  padding: 40rpx;
  text-align: center;
}

.dashboard-empty-text {
  font-size: var(--fs-28);
  color: #94A3B8;
}

.dashboard-empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 80rpx 0;
}

.dashboard-empty-icon {
  font-size: var(--fs-80);
  margin-bottom: 20rpx;
}

/* 加载状态 */
.dashboard-loading {
  display: flex;
  justify-content: center;
  padding: 32rpx 0;
  font-size: var(--fs-26);
  color: #94A3B8;
}

/* 时间范围 picker */
.dashboard-filter-time {
  margin-left: auto;
}

/* uCharts 图表容器 */
.dashboard-person-charts {
  background: #FFFFFF;
  border-radius: 20rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
  padding: 12rpx 0 0 0;
}

.dashboard-person-charts canvas {
  width: 100%;
  height: 500rpx;
}
