/* 本馆日历页面样式 */

.calendar-page {
  min-height: 100vh;
  background: var(--bg-page);
}
.calendar-gradient-header{
  padding: 30rpx 28rpx 32rpx;
}
.calendar-header-top{
}

/* 头部标题 */
.calendar-title {
  font-size: var(--fs-48);
  font-weight: 600;
  color: #FFFFFF;
}

.calendar-subtitle {
  font-size: var(--fs-26);
  color: rgba(255, 255, 255);
}

/* 配置按钮 */
.calendar-config-btn {
  padding: 12rpx 24rpx;
  min-width: 190rpx;
  margin-top: 70rpx;
  border-radius: 24rpx;
  background: rgba(255, 255, 255, 0.2);
  color: #FFFFFF;
  font-size: var(--fs-24);
  font-weight: 400;
  display: flex;
  align-content: center;
  justify-content: center;
  border: 1rpx solid rgba(255, 255, 255, 0.3);
}

/* 日历容器 */
.calendar-container {
  padding: 2rpx 0 0;
}

/* 巴西节假日提示 */
.brazil-holiday-tip {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin: 8rpx 24rpx 0;
  padding: 20rpx 28rpx;
  background: linear-gradient(135deg, var(--bg-soft) 0%, var(--bg-soft) 100%);
  border-radius: 16rpx;
  border: 1rpx solid #BFDBFE;
}

.brazil-holiday-flag {
  font-size: var(--fs-40);
  flex-shrink: 0;
}

.brazil-holiday-text {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.brazil-holiday-pt {
  font-size: var(--fs-28);
  font-weight: 600;
  color: #1E40AF;
}

.brazil-holiday-zh {
  font-size: var(--fs-24);
  color: #3B82F6;
}

/* 配置弹窗遮罩 */
.config-popup-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 9999;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.config-popup-mask.is-visible {
  opacity: 1;
  visibility: visible;
}

/* 配置弹窗容器 */
.config-popup-container {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: var(--bg-card);
  border-radius: 32rpx 32rpx 0 0;
  transform: translateY(100%);
  transition: transform 0.3s ease;
  height: 80vh;
  display: flex;
  flex-direction: column;
  margin: 0 20rpx 0;
}

.config-popup-mask.is-visible .config-popup-container {
  transform: translateY(0);
}

/* 固定头部区域（标题 + 年份选择） */
.config-popup-fixed-header {
  flex-shrink: 0;
}

/* 配置弹窗头部 */
.config-popup-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 32rpx;
  border-bottom: 1rpx solid var(--bg-soft-2);
}

/* 弹窗标题 */
.config-popup-title {
  font-size: var(--fs-34);
  font-weight: 600;
  color: var(--text-primary);
}

.config-popup-close {
  font-size: var(--fs-48);
  color: var(--text-muted);
  line-height: 1;
  padding: 0 8rpx;
}

/* 年份选择 */
.config-field {
  display: flex;
  align-items: center;
  padding: 16rpx 32rpx;
  border-bottom: 1rpx solid var(--bg-soft-2);
}

/* 可滚动区域 */
.config-popup-scroll {
  flex: 1;
  height: 0;
  overflow: hidden;
}

/* 配置表单（scroll-view 内部） */
.config-popup-form {
  padding: 24rpx 32rpx;
}

.config-field-label {
  font-size: var(--fs-28);
  color: var(--text-primary);
  font-weight: 500;
}

.config-field-label-inline {
  font-size: var(--fs-28);
  color: var(--text-primary);
  font-weight: 500;
}

.config-year-picker {
  display: flex;
  align-items: center;
  background: var(--bg-input);
  border-radius: 12rpx;
  padding: 16rpx 24rpx;
  margin: 0 16rpx;
}

.config-year-picker text:first-child {
  font-size: var(--fs-28);
  color: #2563EB;
  font-weight: 600;
}

.config-picker-arrow {
  font-size: var(--fs-22);
  color: var(--text-muted);
  margin-left: 8rpx;
}

/* 节假日列表 */
.config-holidays-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  margin-bottom: 24rpx;
}

.config-holiday-item {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.config-holiday-label {
  font-size: var(--fs-26);
  color: var(--text-tertiary);
  width: 180rpx;
  flex-shrink: 0;
}

.config-date-picker {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-input);
  border-radius: 16rpx;
  padding: 0 24rpx;
  height: 80rpx;
}

.config-date-picker text:first-child {
  font-size: var(--fs-28);
  color: var(--text-primary);
}

.config-delete-btn {
  width: 56rpx;
  height: 56rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #FEE2E2;
  border-radius: 50%;
  font-size: var(--fs-32);
  color: #DC2626;
  flex-shrink: 0;
}

/* 添加按钮 */
.config-add-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 96rpx;
  font-size: var(--fs-32);
  font-weight: 600;
  border-radius: 16rpx;
  border: none;
  background: var(--bg-soft);
  border: 2rpx dashed #2563EB;
  color: #2563EB;
}

.config-add-icon {
  font-size: var(--fs-36);
  font-weight: 600;
}

/* 弹窗底部（固定提交按钮） */
.config-popup-footer {
  padding: 24rpx 32rpx;
  padding-bottom: calc(24rpx + env(safe-area-inset-bottom));
  border-top: 1rpx solid var(--bg-soft-2);
  flex-shrink: 0;
  z-index: 100;
}

/* 提交按钮 */
.config-submit-btn {
  width: 100%;
  height: 96rpx;
  background: linear-gradient(135deg, #2563EB 0%, #4F46E5 100%);
  color: #FFFFFF;
  font-size: var(--fs-32);
  font-weight: 600;
  border-radius: 16rpx;
  border: none;
}

.config-submit-btn[disabled] {
  background: var(--text-muted);
}

/* ========================================
   时间轴日程列表样式
   ======================================== */

/* 日程区域容器 */
.schedule-section {
  margin: 4rpx 8rpx 0;
  background: var(--bg-card);
  border-radius: 24rpx;
  overflow: hidden;
  position: relative;
  box-shadow: 0 8rpx 32rpx rgba(37, 99, 235, 0.08);
}

/* 日期标题 */
.schedule-date-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 28rpx 32rpx;
  border-bottom: 1rpx solid var(--bg-soft-2);
}

.schedule-date-title {
  font-size: var(--fs-30);
  font-weight: 600;
  color: var(--text-primary);
}

.schedule-date-count {
  font-size: var(--fs-26);
  color: var(--text-tertiary);
}

/* 全天日程区域 */
.all-day-section {
  display: flex;
  align-items: flex-start;
  padding: 20rpx 24rpx;
  border-bottom: 1rpx solid var(--bg-soft-2);
  background: var(--bg-input);
}

.all-day-label {
  width: 80rpx;
  font-size: var(--fs-24);
  color: var(--text-tertiary);
  flex-shrink: 0;
  padding-top: 8rpx;
}

.all-day-events {
  flex: 1;
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}

.all-day-event {
  padding: 12rpx 20rpx;
  border-radius: 12rpx;
  border-left: 4rpx solid;
  flex-shrink: 0;
}

.all-day-event-title {
  font-size: var(--fs-26);
  color: var(--text-primary);
  font-weight: 500;
}

/* 时间轴滚动区域 */
.timeline-scroll {
  height: 700rpx;
  position: relative;
}

.timeline-container {
  position: relative;
  min-height: 100%;
}

/* 小时网格 */
.timeline-grid {
  position: relative;
}

.hour-row {
  display: flex;
  align-items: flex-start;
  height: 100rpx;
  border-bottom: 1rpx solid var(--bg-soft-2);
}

.hour-label {
  width: 80rpx;
  font-size: var(--fs-24);
  color: var(--text-muted);
  text-align: center;
  padding-top: 4rpx;
  flex-shrink: 0;
}

/* .hour-line {
  flex: 1;
  height: 1rpx;
  background: var(--border-soft);
  margin-top: 16rpx;
} */

/* 日程条容器 */
.schedule-bars {
  position: absolute;
  top: 0;
  left: 80rpx;
  right: 16rpx;
  bottom: 0;
  pointer-events: none;
  box-sizing: border-box;
}

/* 日程条 */
.schedule-bar {
  position: absolute;
  border-radius: 12rpx;
  padding: 12rpx 16rpx;
  box-sizing: border-box;
  pointer-events: auto;
  overflow: hidden;
  min-height: 50rpx;
  opacity: 0.9;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.1);
}

.schedule-bar-title {
  font-size: var(--fs-24);
  color: #FFFFFF;
  font-weight: 500;
  display: block;
  text-shadow: 0 1rpx 2rpx rgba(0, 0, 0, 0.2);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.schedule-bar-time {
  font-size: var(--fs-22);
  color: rgba(255, 255, 255, 0.9);
  display: block;
  margin-top: 4rpx;
}

/* 当前时间红线 */
.current-time-line {
  margin-top: -10rpx;
  position: absolute;
  left: 70rpx;
  right: 0rpx;
  display: flex;
  align-items: center;
  pointer-events: none;
  z-index: 100;
}

.current-time-label {
  position: absolute;
  left: -66rpx;
  font-size: var(--fs-24);
  color: #EF4444;
  font-weight: 600;
  width: 72rpx;
  text-align: center;
  background: rgba(255, 255, 255, 0.8);
  border: 1rpx solid  #EF4444;
  border-radius: 255rpx;
}

.current-time-dot {
  width: 16rpx;
  height: 16rpx;
  background: #EF4444;
  border-radius: 50%;
  flex-shrink: 0;
  box-shadow: 0 0 8rpx rgba(239, 68, 68, 0.7);
  margin-left: 14rpx;
}

.current-time-bar {
  flex: 1;
  height: 2rpx;
  background: #EF4444;
  box-shadow: 0 0 4rpx rgba(239, 68, 68, 0.5);
}

/* 添加日程按钮 */
.add-schedule-btn {
  position: absolute;
  right: 32rpx;
  bottom: 32rpx;
  width: 100rpx;
  height: 100rpx;
  background: linear-gradient(135deg, #2563EB 0%, #4F46E5 100%);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 24rpx rgba(37, 99, 235, 0.4);
  z-index: 250;
}

.add-schedule-icon {
  font-size: var(--fs-48);
  color: #FFFFFF;
  font-weight: 300;
}

/* ========================================
   日程编辑弹窗样式
   ======================================== */

/* 弹窗遮罩 */
.schedule-popup-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 10000;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.schedule-popup-mask.is-visible {
  opacity: 1;
  visibility: visible;
}

/* 弹窗容器 */
.schedule-popup-container {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: var(--bg-input);
  border-radius: 32rpx 32rpx 0 0;
  transform: translateY(100%);
  transition: transform 0.3s ease;
  height: 85vh;
  display: flex;
  flex-direction: column;
}

.schedule-popup-mask.is-visible .schedule-popup-container {
  transform: translateY(0);
}

/* 弹窗头部 */
.schedule-popup-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 28rpx 32rpx;
  background: var(--bg-card);
  border-bottom: 1rpx solid var(--border-soft);
  border-radius: 32rpx 32rpx 0 0;
  flex-shrink: 0;
}

.schedule-popup-cancel {
  font-size: var(--fs-28);
  color: var(--text-tertiary);
  padding: 8rpx 16rpx;
}

.schedule-popup-title {
  font-size: var(--fs-32);
  font-weight: 600;
  color: var(--text-primary);
}

.schedule-popup-save {
  font-size: var(--fs-28);
  color: #2563EB;
  font-weight: 600;
  padding: 8rpx 16rpx;
}

/* 弹窗滚动区域 */
.schedule-popup-scroll {
  flex: 1;
  height: 0;
  overflow: hidden;
}

.schedule-popup-form {
  padding: 24rpx;
}

/* 表单卡片 */
.schedule-form-card {
  background: var(--bg-card);
  border-radius: 24rpx;
  padding: 8rpx 24rpx;
  margin-bottom: 20rpx;
}

/* 表单字段 */
.schedule-field {
  padding: 20rpx 0;
  border-bottom: 1rpx solid var(--bg-soft-2);
}

.schedule-field:last-child {
  border-bottom: none;
}

.schedule-field-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.schedule-field-clickable {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.schedule-field-label {
  display: flex;
  align-items: center;
  font-size: var(--fs-28);
  color: var(--text-primary);
  font-weight: 500;
}

.schedule-field-value {
  display: flex;
  align-items: center;
  font-size: var(--fs-28);
  color: var(--text-tertiary);
}

.schedule-field-value-content-repeatEndDate{
  margin-top: -40rpx;
}

.schedule-field-value-content{
  padding-right: 20rpx;
}

/* 类型颜色点 */
.schedule-type-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  margin-right: 16rpx;
}

/* 输入框 */
.schedule-input {
  width: 100%;
  height: 80rpx;
  font-size: var(--fs-28);
  color: var(--text-primary);
  background: var(--bg-input);
  border-radius: 16rpx;
  padding: 0 20rpx;
  margin-top: 12rpx;
  box-sizing: border-box;
}

.schedule-textarea {
  width: 100%;
  min-height: 120rpx;
  font-size: var(--fs-28);
  color: var(--text-primary);
  background: var(--bg-input);
  border-radius: 16rpx;
  padding: 16rpx 20rpx;
  margin-top: 12rpx;
  box-sizing: border-box;
}

/* 时间选择行 */
.schedule-time-row {
  display: flex;
  gap: 16rpx;
  margin-top: 12rpx;
}

/* datetime-picker 包装器样式 */
.schedule-picker-wrapper {
  flex: 1;
  background: var(--bg-input);
  border-radius: 16rpx;
  overflow: hidden;
}

.schedule-picker-wrapper.schedule-picker-time {
  flex: 0 0 180rpx;
}

/* 全宽选择器样式 */
.schedule-picker-full {
  background: var(--bg-input);
  border-radius: 16rpx;
  overflow: hidden;
  margin-top: 12rpx;
}

/* 选择器样式 */
.schedule-picker {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-input);
  border-radius: 16rpx;
  padding: 0 20rpx;
  height: 80rpx;
}

.schedule-picker text:first-child {
  font-size: var(--fs-28);
  color: var(--text-primary);
}

.schedule-picker-time {
  flex: 0 0 180rpx;
}

.schedule-picker-arrow {
  font-size: var(--fs-28);
  color: var(--text-muted);
}

/* 删除按钮 */
.schedule-delete-btn {
  text-align: center;
  padding: 32rpx;
  color: #EF4444;
  font-size: var(--fs-28);
  font-weight: 500;
}

/* ========================================
   类型选择弹窗样式
   ======================================== */

.type-popup-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 10001;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.type-popup-mask.is-visible {
  opacity: 1;
  visibility: visible;
}

.type-popup-container {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: var(--bg-card);
  border-radius: 32rpx 32rpx 0 0;
  transform: translateY(100%);
  transition: transform 0.3s ease;
}

.type-popup-mask.is-visible .type-popup-container {
  transform: translateY(0);
}

.type-popup-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 32rpx;
  border-bottom: 1rpx solid var(--bg-soft-2);
}

.type-popup-title {
  font-size: var(--fs-32);
  font-weight: 600;
  color: var(--text-primary);
}

.type-popup-close {
  font-size: var(--fs-48);
  color: var(--text-muted);
  line-height: 1;
}

/* 类型列表 */
.type-list {
  padding: 16rpx 0;
  padding-bottom: calc(16rpx + env(safe-area-inset-bottom));
}

.type-item {
  display: flex;
  align-items: center;
  padding: 28rpx 32rpx;
  transition: background 0.2s ease;
}

.type-item:active {
  background: var(--bg-input);
}

.type-item.is-active {
  background: var(--bg-soft);
}

.type-item-dot {
  width: 20rpx;
  height: 20rpx;
  border-radius: 50%;
  margin-right: 20rpx;
}

.type-item-name {
  flex: 1;
  font-size: var(--fs-30);
  color: var(--text-primary);
}

.type-item-check {
  font-size: var(--fs-32);
  color: #2563EB;
  font-weight: 600;
}

/* ==================== 日程详情弹窗 ==================== */

.detail-popup-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 10000;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.detail-popup-mask.is-visible {
  opacity: 1;
  visibility: visible;
}

.detail-popup-container {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: var(--bg-input);
  border-radius: 32rpx 32rpx 0 0;
  transform: translateY(100%);
  transition: transform 0.3s ease;
  height: 70vh;
  display: flex;
  flex-direction: column;
}

.detail-popup-mask.is-visible .detail-popup-container {
  transform: translateY(0);
}

/* 详情弹窗头部 */
.detail-popup-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 28rpx 32rpx;
  background: var(--bg-card);
  border-bottom: 1rpx solid var(--border-soft);
  border-radius: 32rpx 32rpx 0 0;
  flex-shrink: 0;
}

.detail-popup-close {
  font-size: var(--fs-48);
  color: var(--text-muted);
  line-height: 1;
  padding: 8rpx;
}

.detail-popup-title {
  font-size: var(--fs-32);
  font-weight: 600;
  color: var(--text-primary);
}

.detail-popup-edit {
  font-size: var(--fs-28);
  color: #2563EB;
  font-weight: 600;
}

/* 详情滚动区域 */
.detail-popup-scroll {
  flex: 1;
  height: 0;
  overflow: hidden;
}

.detail-popup-content {
  padding: 24rpx;
  padding-bottom: calc(10rpx + env(safe-area-inset-bottom));
}

/* 详情头部 */
.detail-header {
  background: var(--bg-card);
  border-radius: 24rpx;
  padding: 32rpx;
  margin-bottom: 24rpx;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 16rpx;
}

.detail-type-dot {
  width: 16rpx;
  height: 16rpx;
  border-radius: 50%;
  margin-bottom: 8rpx;
}

.detail-title {
  font-size: var(--fs-36);
  font-weight: 600;
  color: var(--text-primary);
  line-height: 1.4;
}

.detail-type-name {
  font-size: var(--fs-24);
  color: var(--text-tertiary);
  background: var(--bg-soft-2);
  padding: 8rpx 16rpx;
  border-radius: 8rpx;
}

/* 详情区块 */
.detail-section {
  background: var(--bg-card);
  border-radius: 24rpx;
  padding: 24rpx 32rpx;
  margin-bottom: 16rpx;
}

.detail-section-title {
  font-size: var(--fs-24);
  color: var(--text-muted);
  margin-bottom: 12rpx;
  font-weight: 500;
}

.detail-section-value {
  font-size: var(--fs-28);
  color: var(--text-primary);
  line-height: 1.6;
}

/* 时间信息 */
.detail-time-info {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.detail-time-row {
  display: flex;
  align-items: center;
  gap: 24rpx;
}

.detail-time-label {
  font-size: var(--fs-26);
  color: var(--text-tertiary);
  width: 80rpx;
  flex-shrink: 0;
}

.detail-time-value {
  font-size: var(--fs-26);
  color: var(--text-primary);
}

/* 重复信息 */
.detail-repeat-info {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.detail-repeat-label {
  font-size: var(--fs-26);
  color: var(--text-primary);
}

.detail-repeat-end {
  font-size: var(--fs-24);
  color: var(--text-tertiary);
}

/* 日期选择器容器 */
.date-picker-container {
  padding: 32rpx;
  background: var(--bg-input);
  border-radius: 16rpx;
  margin: 16rpx;
}

/* 占位符样式 */
.is-placeholder {
  color: var(--text-muted) !important;
  color: var(--text-tertiary);
  width: 60rpx;
  flex-shrink: 0;
}

.detail-time-value {
  font-size: var(--fs-28);
  color: var(--text-primary);
  font-weight: 500;
}

/* 详情弹窗底部按钮区 */
.detail-popup-footer {
  padding: 5rpx 0 24rpx;
  background: var(--bg-card);
  border-top: 1rpx solid var(--border-soft);
  flex-shrink: 0;
}

.detail-subscribe-btn {
  width: 100%;
  height: 88rpx;
  line-height: 88rpx;
  border-radius: 16rpx;
  background: #2563EB;
  color: #FFFFFF;
  font-size: var(--fs-30);
  font-weight: 500;
  border: none;
}

.detail-subscribe-btn-text{
  display: flex;
  width: 100%;
  height: 100%;
  align-items: center;
  justify-content: center;  
  text-align: center；
}

.detail-subscribe-btn.is-subscribed {
  background: var(--bg-soft-2);
  color: var(--text-tertiary);
}

.detail-subscribe-btn::after {
  border: none;
}
