/* 外出报备页面样式 */

.trip-page {
  min-height: 100vh;
  background: #EEF2FF;
}

/* 头部 - 使用圆角渐变 */
.trip-header {
  padding-bottom: 32rpx;
}

.trip-header-content {
  color: #FFFFFF;
  padding-bottom: 24rpx;
}

.trip-title {
  font-size: 44rpx;
  font-weight: 600;
  margin-bottom: 8rpx;
}

.trip-subtitle {
  font-size: 28rpx;
  opacity: 0.9;
}

/* 状态卡片 - 半透明玻璃效果 */
.trip-status-card {
  background: rgba(255, 255, 255, 0.16);
  border: 1rpx solid rgba(255, 255, 255, 0.22);
  box-shadow: inset 0 1rpx 0 rgba(255, 255, 255, 0.18);
  border-radius: 28rpx;
  padding: 28rpx;
  margin-top: 24rpx;
}

.trip-status-card.is-active {
  background: rgba(255, 255, 255, 0.18);
}

.trip-status-card.is-idle {
  background: rgba(255, 255, 255, 0.12);
}

.trip-status-header {
  display: flex;
  align-items: center;
}

.trip-status-icon {
  width: 64rpx;
  height: 64rpx;
  border-radius: 20rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.16);
  font-size: 32rpx;
  margin-right: 16rpx;
}

.trip-status-info {
  flex: 1;
}

.trip-status-label {
  font-size: 24rpx;
  opacity: 0.8;
  margin-bottom: 4rpx;
}

.trip-status-dest {
  font-size: 32rpx;
  font-weight: 600;
}

.trip-status-detail {
  display: flex;
  gap: 32rpx;
  padding-top: 20rpx;
  margin-top: 20rpx;
  border-top: 1rpx solid rgba(255, 255, 255, 0.15);
}

.trip-status-item {
  display: flex;
  flex-direction: column;
  gap: 4rpx;
}

.trip-status-item-label {
  font-size: 22rpx;
  opacity: 0.7;
}

.trip-status-item-value {
  font-size: 28rpx;
  font-weight: 500;
}

/* 内容区 */
.trip-content {
  padding: 24rpx;
  padding-bottom: 100rpx;
  margin-top: -8rpx;
}

/* 操作按钮 */
.trip-action-section {
  margin-bottom: 32rpx;
}

.trip-action-btn {
  width: 100%;
  height: 100rpx;
  border-radius: 20rpx;
  font-size: 34rpx;
  font-weight: 600;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s ease;
}

.trip-action-btn-depart {
  background: linear-gradient(135deg, #2563EB 0%, #4F46E5 100%);
  color: #FFFFFF;
  box-shadow: 0 8rpx 24rpx rgba(37, 99, 235, 0.3);
}

.trip-action-btn-return {
  background: linear-gradient(135deg, #16A34A 0%, #059669 100%);
  color: #FFFFFF;
  box-shadow: 0 8rpx 24rpx rgba(22, 163, 74, 0.3);
}

.trip-action-btn:active {
  transform: scale(0.98);
}

/* 章节标题 */
.trip-section {
  margin-top: 16rpx;
}

.trip-section-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #1E293B;
  margin-bottom: 20rpx;
  padding-left: 8rpx;
}

/* 月份分组 */
.trip-month-group {
  margin-bottom: 24rpx;
}

.trip-month-title {
  font-size: 26rpx;
  color: #64748B;
  margin-bottom: 16rpx;
  padding-left: 8rpx;
}

/* 列表 */
.trip-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

/* 记录卡片 */
.trip-item {
  background: #FFFFFF;
  border-radius: 20rpx;
  padding: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.04);
}

.trip-item-header {
  display: flex;
  align-items: center;
  margin-bottom: 16rpx;
}

.trip-item-date {
  font-size: 32rpx;
  font-weight: 600;
  color: #1E293B;
  margin-right: 16rpx;
}

.trip-item-time {
  font-size: 26rpx;
  color: #64748B;
  flex: 1;
}

.trip-item-status {
  font-size: 22rpx;
  padding: 6rpx 16rpx;
  border-radius: 8rpx;
  font-weight: 500;
}

.trip-item-body {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.trip-item-dest {
  font-size: 28rpx;
  color: #1E293B;
  font-weight: 500;
}

.trip-item-meta {
  display: flex;
  gap: 20rpx;
}

.trip-item-mode,
.trip-item-companions {
  font-size: 24rpx;
  color: #64748B;
}

/* 空状态 */
.trip-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 80rpx 0;
}

.trip-empty-icon {
  font-size: 80rpx;
  margin-bottom: 20rpx;
}

.trip-empty-text {
  font-size: 28rpx;
  color: #94A3B8;
}

/* 加载状态 */
.trip-loading {
  display: flex;
  justify-content: center;
  padding: 32rpx 0;
}

.trip-loading-text {
  font-size: 26rpx;
  color: #94A3B8;
}

/* 弹窗遮罩 */
.trip-popup-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 100;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
}

.trip-popup-mask.is-visible {
  opacity: 1;
  visibility: visible;
}

/* 弹窗容器 */
.trip-popup-container {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: #FFFFFF;
  border-radius: 32rpx 32rpx 0 0;
  transform: translateY(100%);
  transition: transform 0.3s ease;
  max-height: 80vh;
  overflow-y: auto;
}

.trip-popup-mask.is-visible .trip-popup-container {
  transform: translateY(0);
}

/* 弹窗头部 */
.trip-popup-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 32rpx;
  border-bottom: 1rpx solid #F1F5F9;
  position: sticky;
  top: 0;
  background: #FFFFFF;
  z-index: 1;
}

.trip-popup-title {
  font-size: 34rpx;
  font-weight: 600;
  color: #1E293B;
}

.trip-popup-close {
  font-size: 48rpx;
  color: #94A3B8;
  line-height: 1;
  padding: 0 8rpx;
}

/* 表单 */
.trip-popup-form {
  padding: 32rpx;
  padding-bottom: calc(32rpx + env(safe-area-inset-bottom));
}

.trip-field {
  margin-bottom: 28rpx;
}

.trip-field-label {
  font-size: 28rpx;
  color: #1E293B;
  font-weight: 500;
  margin-bottom: 12rpx;
}

.trip-required {
  color: #DC2626;
}

.trip-field-hint {
  font-size: 22rpx;
  color: #94A3B8;
  margin-top: 8rpx;
}

.trip-input-wrapper {
  background: #F8FAFC;
  border-radius: 16rpx;
  padding: 0 24rpx;
  height: 88rpx;
  display: flex;
  align-items: center;
}

.trip-input {
  flex: 1;
  font-size: 28rpx;
  color: #1E293B;
}

.trip-input-placeholder {
  color: #94A3B8;
}

/* 历史记录列表 */
.trip-history-list {
  margin-top: 12rpx;
  background: #F8FAFC;
  border-radius: 12rpx;
  padding: 16rpx;
}

.trip-history-title {
  font-size: 22rpx;
  color: #94A3B8;
  margin-bottom: 12rpx;
}

.trip-history-items {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}

.trip-history-item {
  background: #FFFFFF;
  border: 1rpx solid #E2E8F0;
  border-radius: 8rpx;
  padding: 10rpx 20rpx;
  font-size: 24rpx;
  color: #475569;
  transition: all 0.2s ease;
}

.trip-history-item:active {
  background: #EEF2FF;
  border-color: #2563EB;
  color: #2563EB;
}

/* 日期时间选择器 */
.trip-datetime-picker {
  display: flex;
  gap: 16rpx;
}

.trip-datetime-picker picker {
  flex: 1;
}

.trip-picker-value {
  background: #F8FAFC;
  border-radius: 16rpx;
  padding: 0 24rpx;
  height: 88rpx;
  display: flex;
  align-items: center;
  font-size: 28rpx;
  color: #1E293B;
}

/* 下拉选择器 */
.trip-picker-wrapper {
  background: #F8FAFC;
  border-radius: 16rpx;
  padding: 0 24rpx;
  height: 88rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.trip-picker-wrapper.is-placeholder .trip-picker-value {
  color: #94A3B8;
}

.trip-picker-arrow {
  font-size: 36rpx;
  color: #94A3B8;
  font-weight: 300;
}

/* 提交按钮 */
.trip-submit-btn {
  width: 100%;
  height: 96rpx;
  background: linear-gradient(135deg, #2563EB 0%, #4F46E5 100%);
  color: #FFFFFF;
  font-size: 32rpx;
  font-weight: 600;
  border-radius: 16rpx;
  border: none;
  margin-top: 20rpx;
}

.trip-submit-btn[disabled] {
  background: #94A3B8;
}
